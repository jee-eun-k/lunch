{
  "compilerOptions": {
    "target": "es6",
    "lib": ["dom", "es6", "dom.iterable"], // Crucial for document, HTMLElement, etc.
    "strict": true,
    "esModuleInterop": true,
    // Deno projects often specify Deno's own type library for better integration:
    // "types": ["https://deno.land/x/deno/cli/types/deno.d.ts"] // Or a more specific version
    // However, for main.ts, "dom" is the most important library.
    "jsx": "react-jsx", // If you were using JSX, for Deno this is common
    "jsxImportSource": "npm:preact" // Example if using Preact with Deno
  },
  "include": [
    "main.ts", // Ensures this file is covered by these settings for the editor
    "server.ts" // And any other TS files
  ],
  "exclude": [
    "node_modules",
    "public/main.js" // Good to exclude build outputs
  ]
}
